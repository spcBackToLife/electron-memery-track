import { test, expect } from '@playwright/test';

test('test', async ({ page }) => {
  await page.getByText('登录游戏').click();
  await page.getByText('生涯').click();
  await page.getByText('战绩').click();
  await page.locator('div').filter({ hasText: /^首页$/ }).nth(1).click();
  await page.locator('#MailBox_Button').click();
  await page.getByRole('button', { name: 'close' }).click();
  await page.locator('#Setting_Button').click();
  await page.getByText('设置').click();
  await page.getByText('通用更新修复安装存储问题反馈关于游戏版本').click();
  await page.getByRole('button', { name: 'close' }).click();
});

我录制了这个过程，你看怎么帮我放到 json 里？每步留一些间隔，而且这个脚本可以在 工具里可以自动给我转化成 steps json 么，并且可以在每个 step 里增加等待时间，我想的是：
1. 测试录制后复制这个到我们这个 tools 里来
2. tools 自动跑脚本转化成 json 里的 steps ，并每步间自动增加间隔
3. 测试也可以改转化后的 json 
4. 然后我这个工具上有一个点击重复执行多次，是不是就可以自动开始执行了，但是这里有个问题，重复执行，是不是这个应用会重启，那是不是得用户提供应用路径，然后脚本自动启动这个应用加上端口么？是不是得在工具里做到这个能力，可以支持 spawn 或者 powershell 应用的启动？
5. 每次录制的会话关闭后，都可以重新启动目标应用，来做下次报告录制吧？会话名你会自己填充么，而且现在有问题就是，好像关闭会话后，工具总是卡住没法继续检测，要重新启动工具才行，这个bug 得修一下？？